version: '3'

services:
  test8ampache85_squashed:
    build:
      context: .
      dockerfile: Dockerfilephp85
    restart: on-failure
    ports:
      - "18581:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ../release-test/8/php85_squashed:/var/www/html/public
      - ./log:/var/log/ampache
      - ./media:/media
      - ./media/art:/mnt/art
      - ./media/music:/mnt/music
      - ./media/podcast:/mnt/podcast
      - ./media/upload:/mnt/upload
      - ./media/video:/mnt/video
