version: '3'

services:
  testampache74:
    build:
      context: .
      dockerfile: Dockerfilephp74
    restart: on-failure
    ports:
      - "17480:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ../release-test/php74:/var/www/html
      - ./log:/var/log/ampache
      - ./media:/media
      - ./art:/mnt/art
      - ./music:/mnt/music
      - ./podcast:/mnt/podcast
      - ./upload:/mnt/upload
      - ./video:/mnt/video

